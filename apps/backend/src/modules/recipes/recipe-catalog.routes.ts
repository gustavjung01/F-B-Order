// Catalog search for recipe ingredients is intentionally registered inside
// recipe-admin.routes.ts before the dynamic /:recipeId route.
// This file remains as a compatibility marker while the feature is consolidated.
export {};
